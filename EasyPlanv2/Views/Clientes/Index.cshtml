@model IEnumerable<EasyPlanv2.Models.Tbl_Clientes>

@{
    ViewBag.Title = "All Clients";
    Layout = "~/Views/Shared/_LayoutDemo.cshtml";
}





<div class="card text-center">

    <div class="card-body">
        <img src="~/Content/logo.jpg" />
        <h5 class="card-title">Clients</h5>
        <a href="@Url.Action("Create","Clientes")" class="btn btn-primary"> Add New </a>
    </div>
</div>


<table class="table table-dark" style="text-align:center">
    <thead>
        <tr>
            <th scope="col">
                Contact
            </th>
            <th scope="col">
                Phone
            </th>
            <th scope="col">
                E-mail
            </th>
            <th scope="col">
                Project
            </th>
            <th><span>Actions</span></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Contacto)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Telefono)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Proyecto)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.NombreCliente }) |
                    @Html.ActionLink("Details", "Details", new { id = item.NombreCliente }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.NombreCliente })
                </td>
            </tr>
        }
    </tbody>
</table>

